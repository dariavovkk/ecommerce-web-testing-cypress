{
  "name": "ecommerce-web-testing-cypress",
  "version": "1.0.0",
  "scripts": {
    "test": "cypress run",
    "open": "cypress open",
    "test:allure": "cypress run --env allure=true",
    "allure:generate": "allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report"
  },
  "devDependencies": {
    "cypress": "^13.0.0",
    "@shelex/cypress-allure-plugin": "^2.38.0"
  }
}